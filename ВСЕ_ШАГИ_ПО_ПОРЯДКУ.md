# Все задачи пошагово — статус

| № | Задача | Статус | Кто/что |
|---|--------|--------|---------|
| 1 | Собрать APK (assembleDebug) | 🔄 Запускалось | В фоне запускалась сборка (JAVA_HOME от Android Studio). Дождитесь завершения или соберите в Android Studio: Build → Build APK(s) |
| 2 | Скопировать APK в site/profi-a.apk | ⬜ | Выполнить после шага 1: запустите **ДОПИСАТЬ_APK_И_ОТПРАВИТЬ.bat** |
| 3 | Закоммитить site/profi-a.apk и index.html | ⬜ | Тот же батник |
| 4 | Push на GitHub | ⬜ | Тот же батник |

---

## Что сделать вам

1. **Дождаться окончания сборки** (если в терминале Cursor ещё идёт `gradlew assembleDebug` — дождитесь сообщения BUILD SUCCESSFUL).

2. **Запустить скрипт** **ДОПИСАТЬ_APK_И_ОТПРАВИТЬ.bat** в корне проекта:
   - он скопирует `app\build\outputs\apk\debug\app-debug.apk` в `site\profi-a.apk`;
   - сделает git add, commit, push.

3. Через 1–2 минуты после push ссылка **https://andrian0123.github.io/PROFI-A/profi-a.apk** заработает.

---

## Если сборка не запускалась или упала

1. Откройте проект в **Android Studio** и соберите: **Build → Build Bundle(s) / APK(s) → Build APK(s)**.
2. Либо в папке `E:\PROFI-A` в терминале задайте Java и запустите:
   ```bat
   set JAVA_HOME=C:\Program Files\Android\Android Studio\jbr
   gradlew.bat assembleDebug
   ```
3. Затем запустите **СОБРАТЬ_И_ПОЛОЖИТЬ_APK_НА_САЙТ.bat** (скопирует APK в site/), после чего **ДОПИСАТЬ_APK_И_ОТПРАВИТЬ.bat** (commit + push).
