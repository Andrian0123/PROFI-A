# Отчёт о невыполненных задачах — PROFI-A

Дата актуализации: 20.02.2026

Задачи, которые на текущий момент реализованы не полностью или не реализованы.

---

## 1. Критичный приоритет

| Блок | Файл/экран | Что не выполнено | Статус |
|------|------------|------------------|--------|
| 3D-сканирование | `RoomScanScreen.kt`, `AddRoomScreen.kt` | Экран отметки участков на плане; визуализация coverage/missing_zones; полная автоподстановка размеров (частично есть: высота, периметр, площадь пола) | В работе |
| Сброс пароля | Backend + Android | Reset password по email/ссылке не реализован | Не начато |

**Выполнено (20.02.2026):** Авторизация (login/register через API, сессия в DataStore); смена пароля, 2FA, удаление аккаунта через API; выход с очисткой сессии.

---

## 2. Средний приоритет

| Блок | Файл/экран | Что не выполнено | Статус |
|------|------------|------------------|--------|
| Поддержка | `SupportScreen.kt` | Отправка только через email intent, нет backend endpoint | Не начато |
| Виды работ | `AddWorkTypesScreen.kt` | Вкладки `GeneralDataPlaceholder` и `MaterialsPlaceholder` остаются заглушками | Не начато |
| Этапы | `StagesScreen.kt` | Экран использует `catalog_placeholder`, реальный контент не реализован | Не начато |
| Локализация UI | Несколько экранов (`AddRoomScreen`, `ProfileScreen`, `SupportScreen` и др.) | Часть пользовательских текстов все еще захардкожена в Kotlin, не вынесена в `strings.xml` | В работе |

---

## 3. Низкий приоритет

| Блок | Файл/экран | Что не выполнено | Статус |
|------|------------|------------------|--------|
| Документация статусов | Корневые отчеты | Есть рассинхрон между старыми отчетами и фактическим состоянием кода | В работе |
| UX/доступность | Несколько экранов | Не все `contentDescription` и служебные подписи унифицированы и локализованы | Не начато |

---

## 4. Краткая сводка

| Категория | Количество |
|-----------|------------|
| Критичный приоритет | 2 |
| Средний приоритет | 4 |
| Низкий приоритет | 2 |
| Всего открытых блоков | 8 |

---

## 5. Примечание по сборке

Предупреждения сборки (kapt, Dagger options, 16 KB alignment, Room schema и т.п.) по умолчанию не считаются блокирующими, если нет ошибок компиляции/линковки.
